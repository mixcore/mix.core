@model Mix.Cms.Lib.ViewModels.Page.ReadMvcViewModel

@foreach (var nav in Model.Modules.OrderBy(n => n.Priority))
{
    var module = nav.Module; 
    if (module != null)
    {
        <partial name="@module.TemplatePath" model="@module" />
    }
}
